// timeAdd = add 2 seconds to the timer
// speedBoost = timer is faster for 10 seconds, but player will get bonus points if they manage to pair
// timeFreeze = timer is freezed for 5 seconds

export const cardEvents = ["timeAdd", "speedBoost", "timeFreeze"];
